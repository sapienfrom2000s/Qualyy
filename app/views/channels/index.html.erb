<%= render partial: "channels/all", locals: { channels: current_user.channels } %>
<%= render 'make_request' %>
<%= link_to 'Cached Videos', videos_path %>
<br>
<br>
<%= render 'new_channel_button' %>
